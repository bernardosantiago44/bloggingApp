<div class="bg-white py-24 sm:py-12 px-0">
  <div class="mx-0 max-w-7xl px-6 lg:px8">
    <div class="mx-auto max-w-2xl lg:mx-0">
      <h2 class="text-4xl font-semibold tracking-tight text-pretty text-gray-900 sm:text-5xl">Welcome to MyBlog</h2>
      <p class="mt-2 text-lg/8 text-gray-600">Created by Bernardo Santiago Marín</p>
    </div>

    <!-- Featured Posts Section -->
    <div class="mx-auto mt-10 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 border-t border-gray-200 pt-10 sm:mt-16 sm:pt-16 lg:mx-0 lg:max-w-none lg:grid-cols-3">
      <% @blogs.each do |blog| %>
        <%= render partial: 'shared/blog_article', locals: { blog: blog } %>
      <% end %>
    </div>
  </div>
</div>